<% @rooms.each do |room| %>
  <%= link_to message_path(room) do %>
    <%= image_tag image_user(room).to_s, class: "icon_image" %>
    <%= opponent_user(room) %>
    <%= most_new_message_preview(room) %>
  <% end %>
<% end %>